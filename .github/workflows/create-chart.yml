      - name: Debug Alliance Bank data
        run: |
          python -c "
          import yfinance as yf, pandas as pd
          s = yf.download('2481.KL','2024-07-01','2025-07-01').Close.dropna()
          print('Alliance 2481.KL rows:', len(s))
          if len(s):
              print('First row:', s.iloc[0], 'on', s.index[0])
              print('Last  row:', s.iloc[-1], 'on', s.index[-1])
          else:
              print('ZERO rows – ticker dead on Yahoo')
          "

name: Update KLSE Bank Chart

on:
  schedule:
    - cron: '0 2 * * 1-5'          # 02:00 UTC Mon-Fri
  workflow_dispatch:               # <-- keeps manual button

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install yfinance pyarrow

      - name: Run update script
        run: python update_chart.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add index.html
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update KLSE bank chart" && git push)
